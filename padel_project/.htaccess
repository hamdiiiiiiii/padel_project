RewriteEngine On

# Keep direct access to existing files (css, js, images, etc.)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything else to front controller
RewriteRule ^ index.php [L,QSA]
